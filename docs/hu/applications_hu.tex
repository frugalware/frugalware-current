\chapter{Felhasználói programok és azok beállításai}
\section{lmsensors}


Az lmsensors egy hardver felügyelõ eszköz ami képes feszültség és hõ értékeket leolvasni az alaplap szenzor csipjérõl. 
\textbf{A {\tt sensors} indítása elõtt, el kell indítani a {\tt sensors-detect}-et mint root, hogy inicializáljon.
Ez automatikusan felismeri a hardverét és információt ad arról, hogy milyen rendszermag modul legyen automatikusan betöltve a rendszer indításakor, hogy problémamentesen mûködjön.
Szintén nézze meg ezt a fejezetet: \ref{chap:init}




\section{apache/httpd}

\textit{Apache beállítása (SSL támogatással)}

0) Elsõ lépések megkövetelik root-jogkört, ezért használja {\tt su -}, hogy megkapja a root-jogokat.


1)Alapértelmezetten az Apache szerver nem fut. Ez megváltoztatható a következõ, 

{\tt service httpd add}

parancsal.

2)Nem akarjuk újraindítani a rendszert, ezért tehát manuálisan indítjuk:

{\tt root@vmhome:~# service httpd start}

{\tt Starting Apache web server (without SSL)        [  OK  ]}

3)Ha SSL támogatást akar:

{\tt root@vmhome:~# cd /etc/httpd/conf/}

{\tt root@vmhome:/etc/httpd/conf# sh mkcert.sh}

{\tt Signature Algorithm ((R)SA or (D)SA) [R]:}

Fogadja el az alapértelmezett RSA aláírás algoritmust.

Néhány mezõt ki kell töltenie, néhányat üresen hagyhat.
Ha '.'-adja meg, akkor a mezõ üres marad.


{\tt 1. Country Name             (2 letter code) [XY]:}

Adja meg a két betûs ország kódot : (pl. US)

{\tt 2. State or Province Name   (full name)     [Snake Desert]:}

Adja meg az államot.

{\tt 3. Locality Name            (eg, city)      [Snake Town]:}

Adjuk meg a városunk, falunk nevét.


{\tt 4. Organization Name        (eg, company)   [Snake Oil, Ltd]:}

Szervezetünk neve jön ide.


{\tt 5. Organizational Unit Name (eg, section)   [Webserver Team]:}

Majd a részleg neve.

{\tt 6. Common Name              (eg, FQDN)      [www.snakeoil.com]:}

\textbf{Fontos:} Itt létezõ címet adjunk meg mert különben, hiba jelentést kap a web böngészõjébe.  
                           

{\tt 7. Email Address            (eg, name@FQDN) [www@snakeoil.dom]:}

Általában ide a webmester email címét adjuk meg(\textit{webmester@domain.hu})

{\tt 8. Certificate Validity     (days)          [365]:}

Általában egy év jó választás.

Utána kiválasztjuk a igazolvány verzióját:

{\tt Certificate Version (1 or 3) [3]:}

Alapértelmezett 3-as rendben lesz, nyomja le az entert.


A következõ lépésben megkezdjük a privát kulcs kódolását.

{\tt Encrypt the private key now? [Y/n]:}

Ezek a kulcsok  \textit{nem} lesznek olvashatóak a felhasználók számára, úgyhogy ezt a lépést kihagyhatja.



A következõ fileok lettek létrehozva:{\tt /etc/httpd/conf/ssl.key/server.key} (ez file maradjon privát),
 {\tt /etc/httpd/conf/ssl.crt/server.crt} és {\tt /etc/httpd/conf/ssl.csr/server.csr}.


4)Most újra kell indítani az apache-t:

{\tt root@vmhome:~# service httpd restart}

{\tt Stopping Apache web server [  OK  ]}

{\tt Starting Apache web server [  OK  ]}

5)  Most meggyõzõdhetünk a parancsok sikereségérõl:

{\tt vmiklos@vmhome:~\$ elinks https://localhost/}

Ez meg kell hogy mutassa a alapértelmezet weboldalt SSL protokolon keresztül. :)

