\chapter{Hotplug podsystém}
\section{udev}

Adresár  {\tt /dev} pod Frugalware je ramdisk a v¹etky zariadenia sú vytvorené automaticky s hotplug podsystémom poèas systémového zavádzania a hlavne s  \textit{udev}. To znamená ¾e pod /dev nebudú nepotrebné zariadenia, ale to znamená keï vytvoríte manuálne zariadenie to bude existova» len do najbli¾¹ieho vypnutia.

Ak chcete prinúti» Frugalware aby vytváral chýbajúce zariadenia "manuálne" poèas zavádzania, musíte editova» {\tt /etc/sysconfig/udev} konfiguraèný súbor. Tento teraz vytvára {\tt /dev/loop*} súbory a {\tt /dev/lp0} (vyzerá to tak ¾e nie v¾dy je vytvorené a je bezpeènej¹ie vytvori» to manuálne)

Udev potrebuje sysfs, a tak to bude pracova» len s jadrami zo série 2.6.x. \textit{{Ne} skú¹ajte  Frugalware s jadrom zo séria
2.4.x s udevom.



\section{Pen/Thumb zariadenia}

Pendrives (nazvané aj ako thumbdrives) sú veµmi dobre podporované cez hotplug skripty a udev. Ak pridáte pendrive do USB 
konektoru, udev vytvorí zariadenie v {\tt /dev}. Zvyèajne pendrives obsahujú jednu partíciu a ich súborový systém je {\tt vfat}. 
Vo väè¹ine prípadov pendrivy budú správa» ako scsi disk. To znamená ¾e mô¾ete k pendrivu pristupova» ako {\tt /dev/sda} a
cez jeho prvú partíciu ako {\tt /dev/sda1}. 
Pridaním nasledujúceho riadku:

{\tt /dev/sda1        /mnt/pendrive    auto        defaults,noauto,user 0 0}

umo¾ní pou¾ívateµom pripoji» ich pendrivy ak zariadenie existuje (a ak pridali to do konektoru).


\section{Digitálne kamery}

Zvyèajne sú dve typy digitálnych kamier ktoré teraz existujú. Po prvé veµa kamier sa správa ako pendrive,
mô¾ete ich µahko pripoji» a kopírova» z nich obrázky.

Iné typy kamier podporujú \textit{Picture Transfer Protocol (PTP)}. Mô¾ete z nich s»ahova» obrázky (a robi» veµa iných vecí) s
{\tt gphoto2}.(Ak to nie je dispozícií na Va¹om systéme, tento príkaz {\tt su -c 'pacman -S gphoto2'} to nain¹taluje na Vá¹ systém.)

\section{{\tt submount/konfigurovateµné a automatické pripojenie externých zariadení}}

{\tt submount} zavádza nový súborový systém, nazvaný {\tt subfs}. Cieµom je aby zabránil pou¾ívateµov manuálne
pripája» a odpája» externé média (CD,floppy atï.). V Frugalware mô¾ete konfigurova» úroveò automatizácie ktoré
mô¾e submount za Vás robi».

Prednastavene (toto je {\tt auto} mód) submount detekuje Va¹e CD/DVD a floppy zariadenia, vytvorením symbolického odkazu
v {\tt /dev}, odstráni staré záznamy a pridá nové do  {\tt /etc/fstab} a pripojí zariadenia s subfs, tak pou¾ívatelia nemusia
pripája» a odpája» zariadenia manuálne.

Ak nemáte rád automatizáciu, mô¾ete vybra» z nasledujúcich módov:
  
Mód {\tt dirs}: vymazáva floppy,cdrom,dvd,dvd,cdrecorder,dvdrecorder adresáre v {\tt /media} a vytvorí tie ktoré sú nutné,
taktie¾ vytvorí záznamy v {\tt /etc/fstab}, ale vy musíte manuálne pripája» zariadenia.
  
Mód {\tt symlinks}: vytvára floppy,cdrom,dvd,cdrecorder,dvdrecorder symbolické odkazy ak je nutné v {\tt /dev}, ale niè viac.

Mód {\tt manual}: niè nerobí, ako v starom dobrom Slackwari.

Mô¾ete nastavi» submountové módy v konfiguraènom súbore {\tt /etc/sysconfig/submount. Taktie¾ mô¾ete konfigurova» extra
mo¾nosti pripojenia ako {\tt iocharset} alebo {\tt codepage}, atï.

