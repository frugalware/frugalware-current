You will find here two types of packages:
* locale packages, for example kde's, aspell's, etc.
* packages that _may_ conflict with other packages in the normal tree, for
  	example, devfsd (conflicts with udev).

Greetings, VMiklos
