diff -Naur boost-1.30.2.orig/boost/config/compiler/gcc.hpp boost-1.30.2/boost/config/compiler/gcc.hpp
--- boost-1.30.2.orig/boost/config/compiler/gcc.hpp	2005-07-21 13:15:57.000000000 +0200
+++ boost-1.30.2/boost/config/compiler/gcc.hpp	2005-07-21 13:17:57.000000000 +0200
@@ -85,11 +85,11 @@
 #endif
 //
 // last known and checked version is 3.4:
-#if (__GNUC__ > 3) || ((__GNUC__ == 3) && (__GNUC_MINOR__ > 4))
-#  if defined(BOOST_ASSERT_CONFIG)
-#     error "Unknown compiler version - please run the configure tests and report the results"
-#  else
-#     warning "Unknown compiler version - please run the configure tests and report the results"
-#  endif
-#endif
+//#if (__GNUC__ > 3) || ((__GNUC__ == 3) && (__GNUC_MINOR__ > 4))
+//#  if defined(BOOST_ASSERT_CONFIG)
+//#     error "Unknown compiler version - please run the configure tests and report the results"
+//#  else
+//#     warning "Unknown compiler version - please run the configure tests and report the results"
+//#  endif
+//#endif
 
