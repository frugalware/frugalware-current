diff -Naur torcs-1.2.4.orig/configure.in torcs-1.2.4/configure.in
--- torcs-1.2.4.orig/configure.in	2005-08-23 15:51:41.000000000 +0200
+++ torcs-1.2.4/configure.in	2006-03-07 00:18:10.000000000 +0100
@@ -77,7 +77,9 @@
 
 CFLAGS="$CFLAGS $ADDCFLAGS"
 CXXFLAGS="$CXXFLAGS $ADDCFLAGS"
-LDFLAGS="$LDFLAGS -L$x_libraries"
+if test "x$x_libraries" != "x"; then
+	LDFLAGS="$LDFLAGS -L$x_libraries"
+fi
 
 dnl Checks for includes (devel part of packages)
 AC_CHECK_HEADER(GL/gl.h,, AC_MSG_ERROR([Can't find GL/gl.h. Look for Mesa devel packages for your distro.]))
