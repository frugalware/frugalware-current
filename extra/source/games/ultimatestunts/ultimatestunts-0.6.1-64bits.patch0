--- simulation/replayer.cpp.original	2006-03-25 01:47:32.000000000 +0100
+++ simulation/replayer.cpp	2006-03-25 01:48:30.000000000 +0100
@@ -107,12 +107,12 @@
 	writel(m_TrackFile);
 
 	//object choices
-	writel(m_ObjectList.size());
+	writel((int)m_ObjectList.size());
 	for(unsigned int i=0; i < m_ObjectList.size(); i++)
 	{
 		CBinBuffer b;
 		m_ObjectList[i].getData(b);
-		writel(b.size());
+		writel((int)b.size());
 		writeBytes(b);
 	}
 
