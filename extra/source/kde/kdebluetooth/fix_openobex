diff -Naur kdebluetooth-1.0_beta1/configure.in kdebluetooth-1.0_beta1-p/configure.in
--- kdebluetooth-1.0_beta1/configure.in	2005-03-20 17:46:50.000000000 +0100
+++ kdebluetooth-1.0_beta1-p/configure.in	2006-04-21 01:17:22.000000000 +0200
@@ -203,28 +203,16 @@
 
 
 dnl Check for openobex library
-# Modified by Simone Gotti for kde-bluetooth on 2003-10-04
+PKG_CHECK_MODULES(OPENOBEX, openobex, , OPENOBEX_CONFIG="check")
+
+if test "$OPENOBEX_CONFIG" = "check" ; then
 
 AC_PATH_PROG(OPENOBEX_CONFIG, openobex-config, no)
 
 if test "$OPENOBEX_CONFIG" = "no" ; then
-	AC_MSG_WARN(openobex-config not found. Pehaps openobex is not installed.)
-	DO_NOT_COMPILE="$DO_NOT_COMPILE kdebluetooth"
+	AC_MSG_WARN(openobex-config not found. Perhaps openobex is not installed.)
 	OPENOBEX_CONFIG="false"
-fi
-
-OPENOBEX_CFLAGS=`$OPENOBEX_CONFIG --cflags`
-openobex_inc_dir=`echo $OPENOBEX_CFLAGS | cut -dI -f2`
-if test -f "$openobex_inc_dir/obex.h" ; then
-    :
 else
-    if test -f "$openobex_inc_dir/openobex/obex.h" ; then
-        OPENOBEX_CFLAGS="$OPENOBEX_CFLAGS/openobex"
-    else
-        DO_NOT_COMPILE="$DO_NOT_COMPILE kdebluetooth"
-    fi
-fi
-OPENOBEX_LIBS=`$OPENOBEX_CONFIG --libs`
 
 min_obex_version=1.0.0
 AC_MSG_CHECKING(for openobex - version >= $min_obex_version)
@@ -261,15 +249,25 @@
 fi
 
 if test "$obex_config_version_ok" != "yes" ; then
-	AC_MSG_WARN(Installed openobex library too old ($obex_config_version))
-	DO_NOT_COMPILE="$DO_NOT_COMPILE kdebluetooth"
-fi
-
+	AC_MSG_WARN(Installed openobex library is too old ($obex_config_version))
+else
+OPENOBEX_CFLAGS=`$OPENOBEX_CONFIG --cflags`
+OPENOBEX_LIBS=`$OPENOBEX_CONFIG --libs`
 
 AC_SUBST(OPENOBEX_CFLAGS)
 AC_SUBST(OPENOBEX_LIBS)
 AC_MSG_RESULT(yes)
 
+fi
+
+fi
+
+fi
+
+if test -z "$OPENOBEX_LIBS" ; then
+DO_NOT_COMPILE="$DO_NOT_COMPILE kdebluetooth"
+fi
+
 CXXFLAGS="$CXXFLAGS $KDE_DEFAULT_CXXFLAGS"
 
 
diff -Naur kdebluetooth-1.0_beta1/kdebluetooth/configure.in.in kdebluetooth-1.0_beta1-p/kdebluetooth/configure.in.in
--- kdebluetooth-1.0_beta1/kdebluetooth/configure.in.in	2005-03-20 17:46:48.000000000 +0100
+++ kdebluetooth-1.0_beta1-p/kdebluetooth/configure.in.in	2006-04-21 01:13:22.000000000 +0200
@@ -129,28 +129,16 @@
 
 
 dnl Check for openobex library
-# Modified by Simone Gotti for kde-bluetooth on 2003-10-04
+PKG_CHECK_MODULES(OPENOBEX, openobex, , OPENOBEX_CONFIG="check")
+
+if test "$OPENOBEX_CONFIG" = "check" ; then
 
 AC_PATH_PROG(OPENOBEX_CONFIG, openobex-config, no)
 
 if test "$OPENOBEX_CONFIG" = "no" ; then
-	AC_MSG_WARN(openobex-config not found. Pehaps openobex is not installed.)
-	DO_NOT_COMPILE="$DO_NOT_COMPILE kdebluetooth"
+	AC_MSG_WARN(openobex-config not found. Perhaps openobex is not installed.)
 	OPENOBEX_CONFIG="false"
-fi
-
-OPENOBEX_CFLAGS=`$OPENOBEX_CONFIG --cflags`
-openobex_inc_dir=`echo $OPENOBEX_CFLAGS | cut -dI -f2`
-if test -f "$openobex_inc_dir/obex.h" ; then
-    :
 else
-    if test -f "$openobex_inc_dir/openobex/obex.h" ; then
-        OPENOBEX_CFLAGS="$OPENOBEX_CFLAGS/openobex"
-    else
-        DO_NOT_COMPILE="$DO_NOT_COMPILE kdebluetooth"
-    fi
-fi
-OPENOBEX_LIBS=`$OPENOBEX_CONFIG --libs`
 
 min_obex_version=1.0.0
 AC_MSG_CHECKING(for openobex - version >= $min_obex_version)
@@ -187,15 +175,25 @@
 fi
 
 if test "$obex_config_version_ok" != "yes" ; then
-	AC_MSG_WARN(Installed openobex library too old ($obex_config_version))
-	DO_NOT_COMPILE="$DO_NOT_COMPILE kdebluetooth"
-fi
-
+	AC_MSG_WARN(Installed openobex library is too old ($obex_config_version))
+else
+OPENOBEX_CFLAGS=`$OPENOBEX_CONFIG --cflags`
+OPENOBEX_LIBS=`$OPENOBEX_CONFIG --libs`
 
 AC_SUBST(OPENOBEX_CFLAGS)
 AC_SUBST(OPENOBEX_LIBS)
 AC_MSG_RESULT(yes)
 
+fi
+
+fi
+
+fi
+
+if test -z "$OPENOBEX_LIBS" ; then
+DO_NOT_COMPILE="$DO_NOT_COMPILE kdebluetooth"
+fi
+
 CXXFLAGS="$CXXFLAGS $KDE_DEFAULT_CXXFLAGS"
 
 
diff -Naur kdebluetooth-1.0_beta1/kdebluetooth/libkobex/kobex.h kdebluetooth-1.0_beta1-p/kdebluetooth/libkobex/kobex.h
--- kdebluetooth-1.0_beta1/kdebluetooth/libkobex/kobex.h	2005-03-20 17:46:48.000000000 +0100
+++ kdebluetooth-1.0_beta1-p/kdebluetooth/libkobex/kobex.h	2006-04-21 01:13:22.000000000 +0200
@@ -30,7 +30,7 @@
 {
 #include <sys/socket.h>
 #include <bluetooth/bluetooth.h>
-#include <obex.h>
+#include <openobex/obex.h>
 }
 
 extern "C" void Obex_handleEvent(obex_t *, obex_object_t *, int, int, int, int);
diff -Naur kdebluetooth-1.0_beta1/kdebluetooth/libkobex/kobexheader.h kdebluetooth-1.0_beta1-p/kdebluetooth/libkobex/kobexheader.h
--- kdebluetooth-1.0_beta1/kdebluetooth/libkobex/kobexheader.h	2005-03-20 17:46:48.000000000 +0100
+++ kdebluetooth-1.0_beta1-p/kdebluetooth/libkobex/kobexheader.h	2006-04-21 01:13:22.000000000 +0200
@@ -20,7 +20,7 @@
 #define KOBEXHEADER_H
 
 extern "C" {
-	#include <obex.h>
+	#include <openobex/obex.h>
 }
 
 class ObexObject;
