post_install()
{
	chkconfig --add rc.ptal
	sed -i 's|#hpoj|hpoj|' /etc/sane.d/dll.conf
}

post_upgrade()
{
	post_install
}

pre_remove()
{
	chkconfig --del rc.ptal
	sed -i 's|hpoj|#hpoj|' /etc/sane.d/dll.conf
}

op=$1
shift

$op $*

# vim: ft=sh
