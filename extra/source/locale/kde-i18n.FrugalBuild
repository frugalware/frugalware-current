#!/bin/bash

# builds and installs all kde localization packages

build() {
  LC_ALL=
  LANG=
  pkg=$1
  shift 1
  cd $pkg
  	makepkg -c $* || exit 1
	pacman -U -f *fpm $*
	# movepkg
  cd ..
}
for i in kde-i18n-*
do
	build $i $*
done
