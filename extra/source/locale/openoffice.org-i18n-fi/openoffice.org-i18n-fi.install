dictlst=/usr/lib/ooo-2.0/share/dict/ooo/dictionary.lst

post_install()
{
	echo "HYPH fi FI hyph_fi_FI" >>$dictlst
}

pre_remove()
{
	tempfile=`mktemp`
	cat $dictlst |grep -v "\(HYPH fi FI hyph_fi_FI\)" >$tempfile
	cat $tempfile >$dictlst
	rm -f $tempfile
}

op=$1
shift
$op $*

# vim: ft=sh
