#! /bin/bash

if [[ "${COICESPATH}" == "" ]] ; then
    export CHOICESPATH="$HOME/Choices:$HOME/.choices:$HOME/.Choices:/usr/share/Choices:/usr/local/share/Choices"
fi

exec /usr/share/Apps/ROX-Filer/AppRun "$@"
