#! /bin/bash

# If $CHOCESPATH is empty source /etc/profile.d/rox.sh
if [[ "${CHOICESPATH}" == "" ]] ; then
    . /etc/profile.d/rox.sh
fi

/usr/bin/rox --pinboard=pinboard
/usr/bin/rox --bottom=bottom
OroboROX
