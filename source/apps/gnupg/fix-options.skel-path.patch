diff -Naur gnupg-1.4.6/configure.ac gnupg-1.4.6-p/configure.ac
--- gnupg-1.4.6/configure.ac	2006-12-05 13:17:02.000000000 +0100
+++ gnupg-1.4.6-p/configure.ac	2007-01-06 21:21:21.000000000 +0100
@@ -1348,7 +1348,6 @@
 #else
 #define G10_LOCALEDIR     "${datadir}/locale"
 #define GNUPG_LIBDIR      "${libdir}/gnupg"
-#define GNUPG_DATADIR     "${datadir}/gnupg"
 #ifdef __VMS
 #define GNUPG_HOMEDIR "/SYS\$LOGIN/gnupg" 
 #else
diff -Naur gnupg-1.4.6/g10/Makefile.am gnupg-1.4.6-p/g10/Makefile.am
--- gnupg-1.4.6/g10/Makefile.am	2006-07-24 15:46:26.000000000 +0200
+++ gnupg-1.4.6-p/g10/Makefile.am	2007-01-06 20:25:55.000000000 +0100
@@ -27,6 +27,7 @@
 
 if ! HAVE_DOSISH_SYSTEM
 AM_CPPFLAGS += -DGNUPG_LIBEXECDIR="\"$(libexecdir)/@PACKAGE@\""
+AM_CPPFLAGS += -DGNUPG_DATADIR="\"$(pkgdatadir)\""
 endif
 
 needed_libs = ../cipher/libcipher.a ../mpi/libmpi.a ../util/libutil.a
diff -Naur gnupg-1.4.6/g10/openfile.c gnupg-1.4.6-p/g10/openfile.c
--- gnupg-1.4.6/g10/openfile.c	2006-11-27 16:58:41.000000000 +0100
+++ gnupg-1.4.6-p/g10/openfile.c	2007-01-06 21:22:52.000000000 +0100
@@ -345,7 +345,7 @@
         errno = EPERM;
       }
     if( !src ) {
-	log_error(_("can't open `%s': %s\n"), fname, strerror(errno) );
+	log_info (_("can't open `%s': %s\n"), fname, strerror(errno) );
 	xfree(fname);
 	return;
     }
@@ -360,7 +360,7 @@
       dst = fopen( fname, "w" );
     umask(oldmask);
     if( !dst ) {
-	log_error(_("can't create `%s': %s\n"), fname, strerror(errno) );
+	log_info (_("can't create `%s': %s\n"), fname, strerror(errno) );
 	fclose( src );
 	xfree(fname);
 	return;
