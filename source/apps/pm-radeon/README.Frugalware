Before you can use this package, you must edit the configuration for it in
the file `/etc/sysconfig/pm-radeon`. After you are done, run this command to
enable it at startup.

----
systemctl enable pm-radeon.service
----
