--- btrfs-progs-5.2.orig/common/format-output.c
+++ btrfs-progs-5.2/common/format-output.c
@@ -280,7 +280,7 @@ void fmt_print(struct format_ctx *fctx,
 
 			localtime_r(&ts, &tm);
 			strftime(tstr, 256, "%Y-%m-%d %X %z", &tm);
-			printf(tstr);
+			printf("%s", tstr);
 		} else {
 			putchar('-');
 		}
