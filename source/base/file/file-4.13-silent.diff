diff -Naur file-4.11.orig/src/apprentice.c file-4.11/src/apprentice.c
--- file-4.11.orig/src/apprentice.c	2004-11-21 00:50:12.000000000 +0100
+++ file-4.11/src/apprentice.c	2004-11-24 00:19:01.201775600 +0100
@@ -163,8 +163,8 @@
 	}
 #ifndef COMPILE_ONLY
 	if ((rv = apprentice_map(ms, &magic, &nmagic, fn)) == -1) {
-		if (ms->flags & MAGIC_CHECK)
-			file_magwarn(ms, "using regular magic file `%s'", fn);
+		/*if (ms->flags & MAGIC_CHECK)
+			file_magwarn(ms, "using regular magic file `%s'", fn);*/
 		rv = apprentice_file(ms, &magic, &nmagic, fn, action);
 		if (rv != 0)
 			return -1;
