From a6a71f87c361bb0be25ee825137fb0634f1abd18 Mon Sep 17 00:00:00 2001
From: crazy <crazy@frugalware.org>
Date: Tue, 28 May 2019 17:21:33 +0200
Subject: [PATCH] fix LDFLAGS sorting

---
 scripts/makepkg | 2 +-
 1 file changed, 1 insertion(+), 1 deletion(-)

diff --git a/scripts/makepkg b/scripts/makepkg
index 78b35b2f..76e7f774 100755
--- a/scripts/makepkg
+++ b/scripts/makepkg
@@ -362,7 +362,7 @@ setflags() {
 	_LDFLAGS="$(/bin/echo ${_LDFLAGS[@]} | tr [:space:] '\n' | awk '!a[$0]++' | tr '\n' ' ')"
 	_CFLAGS="${_MFLAGS} ${_OFLAG} ${_FLAGS} ${_LD_PLUGIN} -pipe"
 	_CXXFLAGS="${_MFLAGS} ${_OFLAG} ${_FLAGS} ${_LD_PLUGIN} -pipe"
-	LDFLAGS_=" ${_LDFLAGS} ${CFLAGS} ${CXXFLAGS}"
+	LDFLAGS_=" ${_LDFLAGS} ${_CFLAGS} ${_CXXFLAGS}"
 
 	CFLAGS="$(/bin/echo ${_CFLAGS[@]} | tr [:space:] '\n' | awk '!a[$0]++' | tr '\n' ' ')"
 	CXXFLAGS="$(/bin/echo ${_CXXFLAGS[@]} | tr [:space:] '\n' | awk '!a[$0]++' | tr '\n' ' ')"
-- 
2.21.0

