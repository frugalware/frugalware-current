# arg 1:  the new package version
# arg 2:  the old package version
post_upgrade()
{
	if [ `vercmp $2 3.3.4` == -1 ] ; then
		sed -i 's/^\([^#].*extra.*\)/#\1/' /etc/pacman.conf
	fi
}

op=$1
shift
$op $*
