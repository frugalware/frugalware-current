--- installpkg.orig	2004-05-01 00:28:32.000000000 +0200
+++ installpkg	2004-06-18 10:58:17.000000000 +0200
@@ -4,12 +4,13 @@
 # installpkg for pacman
 # distributed under GPL License
 
-version=0.4
+version=0.5
 
 if ! [ -e *fpm ]; then
 	echo "No package in this directory!"
 elif [ `ls *.fpm|wc|tr -s ' '|cut -d ' ' -f 3` == 1 ]; then
-	cmda="rm -f ../../../frugalware/`cat FrugalBuild |grep pkgname|sed -n '1 p'|cut -d = -f 2`*fpm"
+	pkgname=`cat FrugalBuild |grep pkgname|sed -n '1 p'|cut -d = -f 2`
+	cmda="rm -f ../../../frugalware/`ls |grep "^$pkgname-[[:digit:]]"`"
 	echo "$cmda"
 	$cmda
 	cmdb="mv `ls *fpm` ../../../frugalware/"
