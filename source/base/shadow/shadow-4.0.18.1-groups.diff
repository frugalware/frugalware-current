--- shadow-4.0.18.1/src/useradd.c.orig	2006-08-04 19:17:24.000000000 +0200
+++ shadow-4.0.18.1/src/useradd.c	2006-08-04 20:27:50.000000000 +0200
@@ -205,11 +205,9 @@
 
 	gid = strtol (grname, &errptr, 10);
 	if (*errptr || errno == ERANGE || gid < 0) {
-		fprintf (stderr,
-			 _("%s: invalid numeric argument '%s'\n"), Prog, grname);
-		exit (E_BAD_ARG);
+		return getgrnam (grname);
 	}
-	return getgrnam (grname);
+	return getgrgid (gid);
 }
 
 static long get_number (const char *numstr)
