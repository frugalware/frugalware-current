post_upgrade()
{
	# Add the new netdev group
        if ! grep -q 'netdev' /etc/group; then
		/usr/sbin/groupadd -g 37 netdev 2>/dev/null
        fi
}

post_remove()
{
	# Remove the backup files
	rm -f etc/{passwd,group,shadow}-
}

op=$1
shift

$op $*
