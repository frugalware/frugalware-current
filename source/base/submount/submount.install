post_install()
{
	/usr/sbin/chkconfig --add rc.submount
	/sbin/depmod -a
}

pre_remove()
{
	/usr/sbin/chkconfig --del rc.submount
}

post_remove()
{
	/sbin/depmod -a
}

op=$1
shift
$op $*

# vim: ft=sh
