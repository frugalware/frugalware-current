# /etc/sysconfig/udev
#
# creates devices that udev left out

# seems that nowadays this already created
#mknod -m 660 /dev/ppp c 108 0

mknod -m 600 /dev/lp0 c 6   0

for i in `seq 0 15`
do
        mknod -m 660 /dev/loop$i b 7 $i
done
