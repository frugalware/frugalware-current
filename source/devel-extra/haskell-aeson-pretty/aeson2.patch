diff -Naur aeson-pretty-0.8.9/aeson-pretty.cabal aeson-pretty-0.8.9.new/aeson-pretty.cabal
--- aeson-pretty-0.8.9/aeson-pretty.cabal	2001-09-09 03:46:40.000000000 +0200
+++ aeson-pretty-0.8.9.new/aeson-pretty.cabal	2022-10-13 21:42:00.891356514 +0200
@@ -1,6 +1,6 @@
 cabal-version:  2.0
 name:           aeson-pretty
-version:        0.8.9
+version:        0.8.10
 license:        BSD3
 license-file:   LICENSE
 category:       Text, Web, JSON, Pretty Printer
@@ -41,27 +41,25 @@
         Data.Aeson.Encode.Pretty
 
     build-depends:
-        aeson ^>= 1.0 || ^>=1.1 || ^>=1.2 || ^>=1.3 || ^>=1.4 || ^>=1.5 || ^>=2.0,
+        aeson ^>= 1.0 || ^>=1.1 || ^>=1.2 || ^>=1.3 || ^>=1.4 || ^>=1.5 || ^>=2.0 || ^>=2.1,
         base >= 4.5,
         base-compat >= 0.9,
         bytestring >= 0.9,
         scientific >= 0.3,
         vector >= 0.9,
         text >= 0.11,
-        unordered-containers >= 0.1.3.0
+        unordered-containers >= 0.2.14.0
 
     if !impl(ghc >= 8.0)
       build-depends:
         semigroups >= 0.18.2
 
     ghc-options: -Wall
-    default-language: Haskell2010
 
 executable aeson-pretty
     hs-source-dirs: cli-tool
     main-is: Main.hs
     other-modules: Paths_aeson_pretty
-    autogen-modules: Paths_aeson_pretty
 
     if flag(lib-only)
         buildable: False
@@ -75,7 +73,7 @@
             cmdargs >= 0.7
 
     ghc-options: -Wall
-    default-language: Haskell2010
+    ghc-prof-options: -auto-all
 
 source-repository head
     type:     git
