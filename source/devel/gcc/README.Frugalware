I have heard I should never remove the gcc package. Is this still true?

No, as of Frugalware-0.3 you can safely remove the gcc package, just never
remove the libstdc++ and gcc-lib packages. (Of course you may if pacman allows
you to do so.)
