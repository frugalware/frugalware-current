post_install() {
	if [ -x /usr/sbin/chkconfig ]; then
		chkconfig --add rc.mono
	fi
}

post_upgrade() {
	post_install
}

pre_remove() 
{
	if [ -x /usr/sbin/chkconfig ]; then
		chkconfig --del rc.mono
	fi
}

op=$1
shift

$op $*
