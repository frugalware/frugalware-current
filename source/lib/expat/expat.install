post_install()
{
    ln -sf /usr/lib/libexpat.so.1.5.0 /usr/lib/libexpat.so.0
}

post_upgrade()
{
    post_install
}

post_remove()
{
    rm -f /usr/lib/libexpat.so.0
}

op=$1
shift

$op $*

# vim: ft=sh
