post_install() {
	if [ -f /etc/fonts/local.conf ]; then
		rm -f /etc/fonts/local.conf
	fi
	/sbin/ldconfig -r .
	echo -n "updating font cache... "
	/usr/bin/fc-cache
	echo "done."
}

post_upgrade() {
	post_install
}

op=$1
shift

$op $*

# vim: ft=sh
