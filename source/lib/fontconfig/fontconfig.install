post_install() {
	if [ -f /etc/fonts/local.conf ]; then
		rm -f /etc/fonts/local.conf
	fi
	/sbin/ldconfig -r .
	/usr/bin/fc-cache
}

post_upgrade() {
	post_install
}

op=$1
shift

$op $*

# vim: ft=sh
