Once you have the `.flv` file you want to share, you need `FlowPlayer.swf` from
`/usr/share/flowplayer` and the following code sniplet:

----
<object type="application/x-shockwave-flash" data="FlowPlayer.swf"
	width="320" height="262">
	<param name="movie" value="FlowPlayer.swf" />
	<param name="flashvars" value="config={videoFile: 'foo.flv', loop: false}" />
</object>
----

// vim: ft=asciidoc
