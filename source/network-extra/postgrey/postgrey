# /etc/sysconfig/postgrey
# configuration file for postgrey

# you may want to set
#   --delay=N   how long to greylist, seconds (default: 300)
#   --max-age=N delete old entries after N days (default: 30)

POSTGREY_ARGS="--inet=127.0.0.1:60000"
