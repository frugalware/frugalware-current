diff -Naur plasma-workspace-5.17.1/startkde/startplasma.cpp plasma-workspace-5.17.1-p/startkde/startplasma.cpp
--- plasma-workspace-5.17.1/startkde/startplasma.cpp	2019-10-22 17:42:08.000000000 +0200
+++ plasma-workspace-5.17.1-p/startkde/startplasma.cpp	2019-10-26 03:24:02.991937907 +0200
@@ -266,13 +266,27 @@
     int exitCode;
     // At this point all environment variables are set, let's send it to the DBus session server to update the activation environment
     if (!QStandardPaths::findExecutable(QStringLiteral("dbus-update-activation-environment")).isEmpty()) {
-        exitCode = runSync(QStringLiteral("dbus-update-activation-environment"), { QStringLiteral("--systemd"), QStringLiteral("--all") });
+        exitCode = runSync(QStringLiteral("dbus-update-activation-environment"), { QStringLiteral("--all") });
     } else {
         exitCode = runSync(QStringLiteral(CMAKE_INSTALL_FULL_LIBEXECDIR "/ksyncdbusenv"), {});
     }
     return exitCode == 0;
 }
 
+bool syncDbusEnvironmentOpional()
+{
+	int outCode;
+	// --systemd
+	// Also update the 'systemd --user' environment
+	// if possible.
+	// --systemd --all always fails , and since --systemd is best effort
+    // lets separate these
+	if (!QStandardPaths::findExecutable(QStringLiteral("dbus-update-activation-environment")).isEmpty()) {
+		outCode = runSync(QStringLiteral("dbus-update-activation-environment"), { QStringLiteral("--systemd") });
+	}
+	return outCode == 0;
+}
+
 void setupFontDpi()
 {
     KConfig cfg(QStringLiteral("kcmfonts"));
diff -Naur plasma-workspace-5.17.1/startkde/startplasma.h plasma-workspace-5.17.1-p/startkde/startplasma.h
--- plasma-workspace-5.17.1/startkde/startplasma.h	2019-10-22 17:42:08.000000000 +0200
+++ plasma-workspace-5.17.1-p/startkde/startplasma.h	2019-10-26 03:03:00.963493881 +0200
@@ -39,6 +39,7 @@
 void cleanupPlasmaEnvironment();
 void cleanupX11();
 bool syncDBusEnvironment();
+bool syncDbusEnvironmentOpional();
 void setupFontDpi();
 QProcess* setupKSplash();
 void setupGSLib();
