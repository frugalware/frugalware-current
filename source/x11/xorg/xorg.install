# arg 1:  the new package version
post_install() {
	echo -n "updating font cache... "
	/sbin/ldconfig -r .
	/usr/X11/bin/fc-cache
	echo "done."
	chkconfig --add rc.xprint
	ln -s /etc/X11/xinit/xinitrc.twm /etc/X11/xinit/xinitrc 
}

# arg 1:  the new package version
# arg 2:  the old package version
post_upgrade() {
	post_install $1
}

# arg 1:  the old package version
pre_remove() {
	/bin/true
}

# arg 1:  the old package version
post_remove() {
	chkconfig --del rc.splash
}


op=$1
shift

$op $*

# vim: ft=sh
