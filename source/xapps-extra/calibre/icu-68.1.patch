--- calibre-5.5.0/src/calibre/utils/icu.c	2020-11-13 02:58:49.000000000 +0100
+++ calibre-5.5.0.new/src/calibre/utils/icu.c	2020-11-14 22:43:30.048838774 +0100
@@ -238,14 +238,14 @@
 
     a = python_to_icu(a_, &asz);
     if (a == NULL) goto end;
-    if (asz == 0) { found = TRUE; goto end; }
+    if (asz == 0) { found = true; goto end; }
     b = python_to_icu(b_, &bsz);
     if (b == NULL) goto end;
 
     search = usearch_openFromCollator(a, asz, b, bsz, self->collator, NULL, &status);
     if (U_SUCCESS(status)) {
         pos = usearch_first(search, &status);
-        if (pos != USEARCH_DONE) found = TRUE;
+        if (pos != USEARCH_DONE) found = true;
     }
 end:
     if (search != NULL) usearch_close(search);
--- calibre-5.5.0/src/calibre/utils/matcher.c	2020-11-13 02:58:49.000000000 +0100
+++ calibre-5.5.0.new/src/calibre/utils/matcher.c	2020-11-14 22:43:52.795009575 +0100
@@ -15,7 +15,6 @@
 #define inline
 #endif
 
-typedef unsigned char bool;
 #define TRUE 1
 #define FALSE 0
 #define MAX(x, y) ((x > y) ? x : y)
