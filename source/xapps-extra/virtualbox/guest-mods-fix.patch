diff -Naur VirtualBox-6.1.12/src/VBox/Additions/linux/Makefile VirtualBox-6.1.12-p/src/VBox/Additions/linux/Makefile
--- VirtualBox-6.1.12/src/VBox/Additions/linux/Makefile	2020-07-11 10:03:31.000000000 +0200
+++ VirtualBox-6.1.12-p/src/VBox/Additions/linux/Makefile	2020-08-23 21:59:33.338789326 +0200
@@ -14,14 +14,14 @@
 # hope that it will be useful, but WITHOUT ANY WARRANTY of any kind.
 #
 
-ifneq ($(KERNELRELEASE),)
+ifneq ($(KBUILD_EXTMOD),)
 
 # Building from kBuild (make -C <kernel_directory> M=`pwd`)
 # or inside a kernel source tree.
 
 obj-m = vboxguest/ vboxsf/ vboxvideo/
 
-else # ! KERNELRELEASE
+else # ! KBUILD_EXTMOD
 
 KBUILD_VERBOSE =
  ifeq ($(KBUILD_VERBOSE),)
@@ -32,6 +32,8 @@
 VBOX_QUIET_SH := set -x;
  endif
 
+PWD ?= $(shell pwd)
+
 all: vboxguest vboxsf vboxvideo
 
 vboxguest:
@@ -46,11 +48,8 @@
 
 vboxsf: vboxguest
 	$(VBOX_QUIET_SH)if [ -d vboxsf ]; then \
-	    if [ -f vboxguest/Module.symvers ]; then \
-	        cp vboxguest/Module.symvers vboxsf; \
-	    fi; \
 	    echo "=== Building 'vboxsf' module ==="; \
-	    $(MAKE) KBUILD_VERBOSE=$(KBUILD_VERBOSE) KBUILD_EXTRA_SYMBOLS=$(abspath vboxsf/Module.symvers) -C vboxsf || exit 1; \
+	    $(MAKE) KBUILD_VERBOSE=$(KBUILD_VERBOSE) KBUILD_EXTRA_SYMBOLS=$(PWD)/vboxguest/Module.symvers -C vboxsf || exit 1; \
 	    if [ -f vboxsf/vboxsf.ko ]; then \
 	        cp vboxsf/vboxsf.ko .; \
 	    else \
@@ -59,13 +58,10 @@
 	    echo; \
 	fi
 
-vboxvideo:
+vboxvideo: vboxguest
 	$(VBOX_QUIET_SH)if [ -d vboxvideo ]; then \
-	    if [ -f vboxguest/Module.symvers ]; then \
-	        cp vboxguest/Module.symvers vboxvideo; \
-	    fi; \
 	    echo "=== Building 'vboxvideo' module ==="; \
-	    $(MAKE) KBUILD_VERBOSE=$(KBUILD_VERBOSE) KBUILD_EXTRA_SYMBOLS=$(abspath vboxvideo/Module.symvers) -C vboxvideo || exit 1; \
+	    $(MAKE) KBUILD_VERBOSE=$(KBUILD_VERBOSE) KBUILD_EXTRA_SYMBOLS=$(PWD)/vboxguest/Module.symvers -C vboxvideo || exit 1; \
 	    if [ -f vboxvideo/vboxvideo.ko ]; then \
 	        cp vboxvideo/vboxvideo.ko .; \
 	    elif [ -f vboxvideo/vboxvideo.o ]; then \
@@ -74,15 +70,21 @@
 	    echo; \
 	fi
 
-install:
+install_vboxguest:
 	$(VBOX_QUIET)$(MAKE) KBUILD_VERBOSE=$(KBUILD_VERBOSE) -C vboxguest install
+
+install_vboxsf:
 	$(VBOX_QUIET_SH)if [ -d vboxsf ]; then \
-	    $(MAKE) KBUILD_VERBOSE=$(KBUILD_VERBOSE) -C vboxsf install; \
+	    $(MAKE) KBUILD_VERBOSE=$(KBUILD_VERBOSE) KBUILD_EXTRA_SYMBOLS=$(PWD)/vboxguest/Module.symvers -C vboxsf install; \
 	fi
+
+install_vboxvideo:
 	$(VBOX_QUIET_SH)if [ -d vboxvideo ]; then \
-	    $(MAKE) KBUILD_VERBOSE=$(KBUILD_VERBOSE) -C vboxvideo install; \
+	    $(MAKE) KBUILD_VERBOSE=$(KBUILD_VERBOSE) KBUILD_EXTRA_SYMBOLS=$(PWD)/vboxguest/Module.symvers -C vboxvideo install; \
 	fi
 
+install: install_vboxguest install_vboxsf install_vboxvideo
+
 clean:
 	$(VBOX_QUIET)$(MAKE) -C vboxguest clean
 	$(VBOX_QUIET_SH)if [ -d vboxsf ]; then \
@@ -105,6 +107,6 @@
 	$(VBOX_QUIET)if [ -f vboxsf.ko ]; then /sbin/insmod vboxsf.ko; fi
 	$(VBOX_QUIET)if [ -f vboxvideo.ko ]; then /sbin/insmod vboxvideo.ko; fi
 
-.PHONY: vboxguest vboxsf vboxvideo all install clean check load
+.PHONY: vboxguest vboxsf vboxvideo all install install_vboxguest install_vboxsf install_vboxvideo clean check load
 
 endif # ! KERNELRELEASE
