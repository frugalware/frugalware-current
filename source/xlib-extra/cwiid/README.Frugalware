=== Module loading

To use your wiimote you have to load module *uninput* with:

------------
# modprobe uninput
------------

To load this module at every start-up, just add *uninput* in _/etc/sysconfig/modules_ file.
