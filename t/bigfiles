#!/bin/bash

. /usr/lib/frugalware/fwmakepkg

if [ "$1" == "--help" ]; then
	echo "files bigger than 100KB"
	exit 1
fi

cd ..

for i in `find _darcs/current -type f`
do
	if [ `stat -c "%s" $i` -gt 100000 ]; then
		echo $i|sed 's|_darcs/current/||'
	fi
done
