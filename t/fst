#!/bin/bash

if [ "$1" ==  "--help" ]; then
	echo "packages with invalid /var/tmp/fst path"
	exit 1
fi

find ../frugalware-x86_64 -type f -name "*.fpm" -print0 | xargs -0 -n 100 -P 16 -I{} \
	bash -c "pacman-g2 -Ql -p {} | grep -q var/tmp/fst && echo {}"
