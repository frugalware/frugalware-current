#!/bin/bash

if [ "$1" ==  "--help" ]; then
	echo "packages with a hardwired python version"
	exit 1
fi

find ../source -name FrugalBuild |xargs grep '/python2.4' \
	|sed 's|.*/\([^/]*\)/FrugalBuild.*|\1|'|sort -u
