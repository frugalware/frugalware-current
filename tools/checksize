#!/bin/bash

# check size, so that we can estimate, if the tree will or will not fit on a cd

cd ..
du -hcs `ls|grep -v source|grep -v extra`

# this display the size of generated iso images

# ls -lh|grep ^-.*current|sed 's/.*t \(.*\) 200.*/\1/'
